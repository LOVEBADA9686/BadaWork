2021-10-06
-----------------------------------

html : DOM Tree : html 루트태그를 중심으로 배열로 구성된다.

wire frame <table border="1"></table>

CSS : 위에서 아래로 흐른다. 중복 허용, 셀렉터(아이디, 클래스, 태그, 상속..)

javaScript jQuery(함수 체이닝) : 이벤트

xml : 태그의 정의를 알아야 한다. : 사용자가 태그를 만든다.
	  데이터 이동
	  환경 설정 파일 사용
	  apache-tomcat
	  		server.xml
				connector
				context
			contents.xml
				resource
			web.xml
				session
				mime
			Context/WEB-INF/web.xml
				welcom-file

json : 